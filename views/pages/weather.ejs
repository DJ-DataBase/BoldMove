<!DOCTYPE html>
<html>
  <head>
       <%- include('../layout/head') %>
  </head>
<body>
  <header>
      <%- include('../layout/header') %>
  </header>
  <h3>~Weather~</h3>
<main>
  <div class="main-weather"></div>
    <ul class="weather-results">
      <li class="weatherSection">
        <h4>Today's Forecast</h4>
        <h3>Currently: <%= weather.current_temp %>° F</h3>
          <div id="weatherParagraph">
            <p><span>Summary:</span> <%= weather.current_summary %></p>
            <p><span>Chance of Precipitation:</span> <%= weather.current_precip %>%</p>
            <p><span>Humidity:</span> <%= weather.current_humidity %>%</p>
            <p><span>Cloud Cover:</span> <%= weather.current_cloud_cover %>%</p>
            <p><span>Wind Speed:</span> <%= weather.current_wind_speed %> MPH</p>
            <p><span>Average Visibility:</span> <%= weather.current_visibility %> Miles</p>
          </div>
      </li>
      <li class="weatherSection">
        <h4>Tomorrow's Forecast</h4>
        <h3>High: <%= weather.tomorrow_high %>° F</h3>
        <h3>Low: <%= weather.tomorrow_low %>° F</h3>
          <div id="weatherParagraph">
            <p><span>Summary:</span> <%= weather.tomorrow_summary %></p>
            <p><span>Chance of Precipitation:</span> <%= weather.tomorrow_precip %>%</p>
            <p><span>Humidity:</span> <%= weather.tomorrow_humidity %>%</p>
            <p><span>Cloud Cover:</span> <%= weather.tomorrow_cloud_cover %>%</p>
            <p><span>Wind Speed:</span> <%= weather.tomorrow_wind_speed %> MPH</p>
            <p><span>Average Visibility:</span> <%= weather.tomorrow_visibility %> Miles</p>
          </div>
      </li>
    </ul>
  </div>
  </main>
  <footer>
      <%- include('../layout/footer') %>
  </footer>
</body>